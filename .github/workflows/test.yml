name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-slim, ubuntu-latest, ubuntu-22.04, macos-latest, windows-latest, windows-2025, windows-11-arm, macos-26-intel]

    steps:
      - uses: actions/checkout@v6
    
      - name: Fail the job
        run: exit 1

      - name: Debug failed job
        if: ${{ failure() }}
        uses: ./
        with:
          ports: "22,3389,5900"
          new-password: ${{ secrets.DEBUG_PASSWORD }}
