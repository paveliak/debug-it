name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04-firewall]

    steps:
      - uses: actions/checkout@v6
    
      - name: Fail the job
        run: sudo cat /etc/fstab

      - name: Debug failed job
        if: ${{ failure() }}
        uses: ./
        with:
          ports: "22,3389,5900"
          new-password: ${{ secrets.DEBUG_PASSWORD }}
